apiVersion: v1
kind: ConfigMap
metadata:
  name: catalogue
  namespace: roboshop
  # these labels are replicaset labels
  labels:
    project: roboshop
    tier: backend
    component: catalogue
data:
  MONGO: "true"  
  MONGO_URL: {{ .Values.configMap.MONGO_URL }}

