name: feature-pipeline
on: 
  push:
  workflow_dispatch:

jobs:
  feature-pipeline:
    if: github.ref_name != 'main'
    uses: Naga-Sai-Prasanna/workflows/.github/workflows/nodejs-feature-pipeline.yaml@main
    with: 
      project: 'roboshop'
      component: 'catalogue'
 